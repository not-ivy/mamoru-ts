{
  "name": "mamoru",
  "packageManager": "yarn@4.6.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "scripts": {
    "dev": "yarn workspaces foreach --exclude mamoru -Api run dev",
    "deploy": "yarn workspace foreach --exlude mamoru -A run deploy"
  }
}
